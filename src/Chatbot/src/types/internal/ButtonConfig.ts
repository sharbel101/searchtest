import React from 'react';

/**
 * Defines the button configurations for header, chat input and footer.
 */
export type ButtonConfig = {
  header: React.JSX.Element[];
  chatInput: React.JSX.Element[];
  footer: React.JSX.Element[];
};
